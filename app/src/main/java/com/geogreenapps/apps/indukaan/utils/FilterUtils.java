package com.geogreenapps.apps.indukaan.utils;

public class FilterUtils {

    /*public  static FilterView filter_setup(View rootView) {

        Drawable calender_icon = new IconicsDrawable(rootView.getContext())
                .icon(CommunityMaterial.Icon.cmd_calendar)
                .color(ResourcesCompat.getColor(rootView.getContext().getResources(), R.color.defaultWhiteColor, null))
                .sizeDp(18);

        Drawable location_icon = new IconicsDrawable(rootView.getContext())
                .icon(CommunityMaterial.Icon.cmd_map_marker)
                .color(ResourcesCompat.getColor(rootView.getContext().getResources(), R.color.defaultWhiteColor, null))
                .sizeDp(18);

        FilterView filterBar = rootView.findViewById(R.id.filter_layout);

        filterBar.setupLeft(calender_icon, rootView.getContext().getResources().getString(R.string.date_filtre));
        filterBar.setupRight(location_icon, rootView.getContext().getResources().getString(R.string.geo_Filter));

        return filterBar;
    }*/
}
